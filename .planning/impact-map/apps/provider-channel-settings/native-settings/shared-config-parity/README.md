# Shared Native Config Parity

Coverage: `exceptioned-deep-partial`
Freshness: 2026-05-09 GitNexus/code-review-graph bounded slice plus targeted validation where platform checks were available

## Scope

Shared provider/channel/config expectations across native apps and web/docs surfaces.

## Leaf status

Split from the former broad `native-settings/` leaf during G007. Full parity remains exceptioned until native macOS/iOS and web checks run together.

## Detail files

- `file-roles.md`: concrete path ownership and likely impact radius.
- `code-paths.md`: behavior paths to trace before changing code.
- `change-to-test.md`: smallest validation ladder and exception rules.
