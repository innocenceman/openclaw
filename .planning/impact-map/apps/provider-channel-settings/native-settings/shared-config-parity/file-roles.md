# Shared Native Config Parity File Roles

Coverage: `exceptioned-deep-partial`

| Role | Paths |
| --- | --- |
| Primary owners | Native settings docs plus `ui/provider-channel-settings/web-settings/` and docs sync leaves |
| Shared dependencies | Config schema, channel/provider metadata, gateway auth/session contracts, and web settings parity. |
