# Native Provider and Channel Settings

Coverage: `exceptioned-deep-partial`
Freshness: 2026-05-09 G007 split and validation evidence

## Scope

Leaf module for the native provider/channel settings surfaces anchored in macOS settings views, iOS settings/gateway persistence, and Android connect/settings screens.

## Leaf status

This is a Wave 3 priority completion unit because it is the native side of provider/channel settings parity and persisted connection/config state.

## Detail files

- `file-roles.md`: concrete file/path ownership and likely impact radius.
- `code-paths.md`: behavior paths to trace before changing code.
- `change-to-test.md`: smallest validation ladder for common changes.
