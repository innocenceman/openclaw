# macOS Native Settings File Roles

Coverage: `exceptioned-deep-partial`

| Role | Paths |
| --- | --- |
| Primary owners | `apps/macos/Sources/OpenClaw/ConfigSettings.swift`, `ChannelsSettings*.swift`, `ChannelConfigForm.swift`, `SettingsRootView.swift` |
| Shared dependencies | Config schema, channel/provider metadata, gateway auth/session contracts, and web settings parity. |
