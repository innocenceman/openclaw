# Mobile Native Settings File Roles

Coverage: `exceptioned-deep-partial`

| Role | Paths |
| --- | --- |
| Primary owners | `apps/ios/Sources/*Gateway*`, `apps/ios/Tests/*Gateway*`, `apps/android/app/src/main/java/ai/openclaw/app/ui/*Settings*`, `ConnectTabScreen.kt` |
| Shared dependencies | Config schema, channel/provider metadata, gateway auth/session contracts, and web settings parity. |
