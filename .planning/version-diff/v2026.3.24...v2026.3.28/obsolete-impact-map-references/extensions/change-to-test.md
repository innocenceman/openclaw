# Extensions Change-to-Test Matrix

Coverage: `partial`
Freshness: 2026-05-07 repo-native inspection only

| Change area                                                   | First validation                                                                                                                                                                                                                                                   | Escalate when                                                                                                                                                                 |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Manifest ID, channel ID, provider metadata, or package naming | `pnpm test -- src/plugins/bundled-plugin-naming.test.ts src/plugins/bundled-plugin-metadata.test.ts src/plugins/contracts/catalog.contract.test.ts` plus `pnpm check:bundled-plugin-metadata` when metadata generation can drift                                   | package exports, generated metadata, or published plugin behavior changes; then run `pnpm build`                                                                              |
| Extension SDK/import boundary                                 | `pnpm test -- test/extension-plugin-sdk-boundary.test.ts test/plugin-extension-import-boundary.test.ts` plus `pnpm lint:extensions:no-src-outside-plugin-sdk` / `pnpm lint:extensions:no-plugin-sdk-internal` / `pnpm lint:extensions:no-relative-outside-package` | public SDK surface or lazy-loading boundary changes; then run `pnpm plugin-sdk:api:check` and `pnpm build`                                                                    |
| Plugin loader/runtime/discovery contract                      | `pnpm test:contracts:plugins`                                                                                                                                                                                                                                      | loader, registry, auth, provider, wizard, or runtime contracts change across multiple plugin types                                                                            |
| Channel plugin behavior                                       | `pnpm test:contracts:channels` plus nearest targeted extension test, then `pnpm test:channels` when shared channel behavior changes                                                                                                                                | inbound/outbound payloads, command gating, group policy, setup/status, or shared routing changes                                                                              |
| Single extension implementation                               | `pnpm test -- extensions/<id>` or nearest colocated extension test via the repo wrapper                                                                                                                                                                            | the extension touches shared SDK helpers, channel/plugin contracts, runtime dependencies, or build output                                                                     |
| Extension package runtime deps                                | `pnpm test -- test/scripts/stage-bundled-plugin-runtime-deps.test.ts src/plugins/stage-bundled-plugin-runtime.test.ts`                                                                                                                                             | `dependencies`/`peerDependencies`/install behavior changes; then `pnpm stage:bundled-plugin-runtime-deps`, release plugin checks, or `pnpm test:docker:plugins` may be needed |
| Plugin SDK public API                                         | `pnpm plugin-sdk:api:check` and `pnpm plugin-sdk:check-exports`                                                                                                                                                                                                    | published surfaces, generated dts, or package exports change; `pnpm build` is a hard gate                                                                                     |
| Build/lazy-loading/module boundary                            | `pnpm build` and inspect for dynamic import warnings                                                                                                                                                                                                               | static and dynamic imports mix for the same module in production code paths                                                                                                   |

## Validation evidence

No product tests/builds were run for Wave 2. The matrix is based on current `package.json` scripts, contract test paths, and repo-native file inspection.
