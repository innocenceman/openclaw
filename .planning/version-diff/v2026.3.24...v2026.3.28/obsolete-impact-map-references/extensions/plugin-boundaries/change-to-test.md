# Plugin Boundaries Change-to-Test Matrix

Coverage: `deep-partial`
Freshness: 2026-05-08 repo-native inspection only

| Change type                                                                                 | First validation                                                                                                                                                                                         | Escalation trigger                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Manifest `id`, package name, channel id, provider id, or install metadata changed           | `pnpm test -- src/plugins/bundled-plugin-naming.test.ts src/plugins/bundled-plugin-metadata.test.ts src/plugins/contracts/catalog.contract.test.ts`                                                      | Run `pnpm check:bundled-plugin-metadata`; `pnpm build` if generated metadata or published surfaces change.                                                                                                                     |
| Manifest auth/config/UI metadata changed                                                    | `pnpm test -- src/plugins/contracts/auth.contract.test.ts src/plugins/contracts/auth-choice.contract.test.ts src/plugins/contracts/wizard.contract.test.ts src/plugins/contracts/shape.contract.test.ts` | Also run config/docs/generated checks if public config/help docs can drift.                                                                                                                                                    |
| Extension production import boundary changed                                                | `pnpm test -- test/extension-plugin-sdk-boundary.test.ts test/plugin-extension-import-boundary.test.ts`                                                                                                  | Add `pnpm lint:extensions:no-src-outside-plugin-sdk`, `pnpm lint:extensions:no-plugin-sdk-internal`, `pnpm lint:extensions:no-relative-outside-package`, and `pnpm lint:plugins:no-extension-imports` for broad boundary work. |
| Support-only helper path changed (`extensions/shared/**`, `extensions/anthropic-vertex/**`) | `pnpm test -- test/extension-plugin-sdk-boundary.test.ts test/plugin-extension-import-boundary.test.ts`                                                                                                  | Add targeted consumer tests for affected channel/provider leaves when helper behavior changes, because these paths do not have their own manifest-backed leaf row.                                                             |
| Public `openclaw/plugin-sdk/*` seam changed                                                 | `pnpm plugin-sdk:api:check` and `pnpm plugin-sdk:check-exports`                                                                                                                                          | Approved `pnpm plugin-sdk:api:gen`; `pnpm build` if dts/build output changes.                                                                                                                                                  |
| Entrypoint helper or loader behavior changed                                                | `pnpm test:contracts:plugins`                                                                                                                                                                            | Run targeted extension tests and `pnpm test:extensions` when multiple plugin types are affected.                                                                                                                               |
| Channel entry boundary changed                                                              | `pnpm test:contracts:channels`                                                                                                                                                                           | Run `pnpm test:channels` and affected extension channel tests when inbound/outbound/setup behavior changes.                                                                                                                    |

## Validation evidence

No product tests/builds were run while deepening this slice. The commands above were selected from current root `package.json` scripts plus repo-native test-path inspection.
